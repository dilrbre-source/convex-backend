interface:
  display_name: "Convex Backend"
  short_description: "Repo-specific patterns and workflows for convex-backend"
  default_prompt: "Use the convex-backend repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true